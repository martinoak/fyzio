{if !empty(config('fyzio.announcement'))}
    <div style="background-color: {config('fyzio.background')|noescape};width: 100%;font-weight:bold;text-align:center;color:white;padding:10px 0">
        {config('fyzio.announcement')|noescape}
    </div>
{/if}
